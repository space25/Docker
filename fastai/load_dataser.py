from fastai.vision import *
from fastai.metrics import error_rate

untar_data(URLs.PETS)
untar_data(URLs.BIWI_HEAD_POSE)
untar_data(URLs.ADULT_SAMPLE)
untar_data(URLs.HUMAN_NUMBERS)
untar_data(URLs.LSUN_BEDROOMS)
untar_data(URLs.MNIST)
untar_data(URLs.ML_SAMPLE)
untar_data(URLs.CAMVID)